name: Build Dania Fedorina Makeup TV Native v2

on:
  push:
    branches: ["makeup-tv-app"]
    paths:
      - 'makeup-tv-v2/**'
      - '.github/workflows/build-makeup-tv-v2.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'

      - name: Reassemble native Android TV project
        run: |
          rm -rf makeup-tv-v2/build-src
          mkdir -p makeup-tv-v2/build-src
          cat makeup-tv-v2/parts/v2.part* > /tmp/makeup-tv-v2.b64
          test "$(wc -c < /tmp/makeup-tv-v2.b64)" -eq 31216
          base64 -d /tmp/makeup-tv-v2.b64 > /tmp/makeup-tv-v2.zip
          unzip -t /tmp/makeup-tv-v2.zip
          unzip -q /tmp/makeup-tv-v2.zip -d makeup-tv-v2/build-src

      - name: Build debug APK
        working-directory: makeup-tv-v2/build-src
        run: gradle :app:assembleDebug --stacktrace

      - name: Collect APK
        run: |
          mkdir -p dist
          cp makeup-tv-v2/build-src/app/build/outputs/apk/debug/app-debug.apk dist/Dania-Fedorina-Makeup-TV-v2.0.0-native.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Dania-Fedorina-Makeup-TV-v2.0.0-native
          path: dist/Dania-Fedorina-Makeup-TV-v2.0.0-native.apk
          if-no-files-found: error
